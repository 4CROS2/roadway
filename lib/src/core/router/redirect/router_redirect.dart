import 'dart:async';
import 'package:flutter/material.dart';
import 'package:go_router/go_router.dart';

class RouterRedirect {
  // Aquí puedes inyectar tus casos de uso o Blocs usando get_it si lo necesitas.
  // Ejemplo: final AuthBloc authBloc = getIt<AuthBloc>();

  static FutureOr<String?> call(BuildContext context, GoRouterState state) {
    return null;
  }
}
